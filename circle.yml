version: 2
jobs:
  build:
    docker:
      - image: fedora:latest
    working_directory: ~/Hardcode-Tray
    steps:
      - run: >
          dnf -y install
          gettext
          gtk3-devel
          meson
      - checkout
      - run: pip3 install --user --upgrade pip
      - run: pip3 install --user cairosvg pylint jsonschema
      - run: meson builddir
      - run: ninja -C builddir install
      - run: python3 ./tests/database/database.py
      - run: sh ./tests/pylint.sh
